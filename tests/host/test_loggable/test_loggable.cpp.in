#include "loggable.hpp"

#include "@FILE_WITH_LOGGABLE_CLASS_DEFINITION@"

struct LoggableTest
{
    explicit LoggableTest(Loggable auto& logger)
    {
        logger.log(LogLevel::error);
    }
};

int main()
{
    Logger logger;
    LoggableTest loggable_test{logger};
    return 0;
}